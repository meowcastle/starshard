.DS_Store
.thumbnail
.claude/settings.local.json

# backend
starshard-api/node_modules/
starshard-api/.env
starshard-api/run.log

# site tooling: tests, and the vendored dc-runtime CDN deps for offline smoke runs
node_modules/
vendor/
package-lock.json

# raw Claude Design exports/comps land here before anything gets promoted into
# the real Code-owned modules — see OWNERSHIP.md and docs/archive/REVIEW.md §1.1
design/incoming/

# Two raw exports landed at the repo root instead of design/incoming/ (Aug
# 2026 cleanup) — ignored by name rather than moved, since the newest one is
# still the live page's reference until a real Design handoff replaces it.
# Put future exports in design/incoming/ and this won't be needed again.
/Star Shard Astrology Website (1)/
/Starshard V3 (1)/

# design-system/ is a real buildable package (source is committed); its
# build output regenerates from src/ and shouldn't be
design-system/dist/
design-system/preview.html

# /design-sync scratch: staged converter scripts + their own node_modules,
# regenerated bundle output, and machine verification state. The durable
# set (config.json, NOTES.md, conventions.md, previews/, overrides/) is
# NOT covered by this and stays committed.
design-system/.ds-sync/
design-system/ds-bundle/
design-system/.design-sync/.cache/
design-system/.design-sync/learnings/
design-system/.design-sync/node_modules

.projects/cache
.projects/vault
.projects/state.test.json
.projects/state.local.test.json
.env
.env.*
!.env.example
